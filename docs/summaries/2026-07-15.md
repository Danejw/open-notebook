# Daily Work Summary — 2026-07-15

## Overview

A full-stack day across four major tracks: persistent chat queue and A2UI generative surfaces, complete insights removal with migration 39 verification, a sustained frontend architecture remediation (UI-001 through UI-061, score 64 → 87), dead-code cleanup (audit score 69 → 81), and a new **Collections** feature (migration 40–41, API, graph context, chat picker, user docs). Branch `dev` is clean at end of day.

## Completed Work

### Persistent chat queue
- Implemented durable chat queue across API (`chat_queue_service`, `chat_queue` router), domain (`chat_queue` / `chat_queue_models`), worker (`queue_runner`, `chat_queue_commands`), and SurrealDB migrations 36–37.
- Frontend: `ChatQueuePanel`, `useChatQueue`, `chat-queue` API client/types, i18n keys, and project/source chat hook integration.
- Added extensive tests (`test_chat_queue_api`, domain, worker, worker safety) and user/config/troubleshooting docs.
- Added Cursor rule `api-server-running.mdc` for local API/worker health checks.
- Smoke verified via `scripts/smoke_chat_queue_a2ui.py`.

### A2UI chat surfaces
- Integrated A2UI v0.9 over AG-UI `CUSTOM` events with feature flags `A2UI_CHAT_ENABLED` / `NEXT_PUBLIC_A2UI_CHAT`.
- Backend emit path (`a2ui_emit.py`) and chat graph wiring; frontend catalog, surface store, parse/hydrate/policy, `A2uiMessageSurface`, Cos components.
- Replaced context-confirm flow with `AskUser`; added inline A2UI parsing/display helpers and agent catalog/component docs under `docs/ai-protocols/a2ui/` (originally `docs/a2ui/`).

### Artifacts + chat UX
- Enhanced artifact model and chat integration (migration 38, API models, editor dialog, `useProjectChat` / `ChatColumn` wiring).
- Removed artifact “clear” UX from active artifact bar / chat composer / context strip / message list.

### Insights feature removal
- Deleted insights API router/service/client hooks/dialogs and related domain/graph/context/prompt wiring.
- Migration 39 (+ down) for schema cleanup; embedding/source commands and promotion paths updated accordingly.
- Frontend: removed insight dialogs/hooks/API; updated context indicators, source detail, locales, and source references.
- Residual docs/locale/CLAUDE.md copy sweep so product language is artifacts → notes (not insights).
- Migration 39 verified on local SurrealDB via `scripts/verify_migration_39.py` (`source_insight` absent, version 39).
- Only intentional compatibility: stored context mode `"insights"` normalizes to full content.

### Frontend architecture remediation (UI-001–UI-061)
- **Baseline (PR #2, UI-001–UI-025):** Shared foundations — `ConfirmDialog`, `FieldError`, `PageRefreshButton`, `listActionTriggerClassName`, Radix `Select`/`Input`, `SettingSelectField`, `ChatModelOverrideDialog`, `ProjectActionsMenu`, podcast panel shells, `useChatStreamingBuffer`, shared chat session/SSE/queue helpers, `ChatPanel` subcomponents, foundation unit tests, sidebar a11y/i18n. Merged via PR #2.
- **Wave 2 (UI-026–UI-040):** `PickerDialogShell`, `FormDialogShell`, `useListSelection`, `CompactListRow`, `PageError`, `EmptyState` subtle variant, `useSource` in source detail, api-keys page split, `ModelSelector` extensions, destructive design tokens, sidebar `aria-current` / Link nesting fix, shared chat send runtime (`useChatSendTurn` path).
- **Wave 3 (UI-041–UI-048):** `ToolPicker` on `PickerDialogShell`; `useSelectableRow`; `bindChatPanelProps`; `useChatRuntime` thin adapters; `CheckboxList`; `FormDialogShell` on artifact rename; `CredentialItem` aria-labels; shell + `useChatSendTurn` tests.
- **Wave 4 (UI-049–UI-061):** `AddExistingSourceDialog` picker shell + checkbox rows; `McpAuthFields` extraction; `McpToolRiskBadge`; `PageError` / `ChatPanelSkeleton` adoption; `ArtifactListRow` + `ArtifactViewerDialog` from `ArtifactsColumn`; `bindCommonChatPanelProps` DRY; `CredentialItem` i18n; `ListSelectionBar` tests.
- **Score progression:** 64 → 72 → 82 → **87/100** (audits in `docs/audits/`). 41 frontend test files / 269 tests at 87 score.

### Dead code and API cleanup
- Published dead-code audit: efficiency **81/100** (up from 69); `docs/audits/dead_code_redundancy_audit.md`.
- Removed unused API service layer files (`artifacts_service`, `chat_service`, `notes_service`, `project_service`, `models_service`, `embedding_service`, `episode_profiles_service`, `podcast_api_service`, `context_service`, legacy `api/client.py`, etc.); routers call domain directly.
- Added CI lint jobs for unused imports (backend ruff F401, frontend knip).
- Added workspace Cursor rules: `absolute-imports.mdc`, `dry-principles.mdc`, `strict-typing.mdc`.
- Merged `dead-code` branch into frontend consistency work.

### Collections feature (new)
- Full-stack Collections: curated URL bundles attachable to project/source chat (mirrors Skills pattern).
- Migrations **40** (`collection` + `collection_item`) and **41** (`ChatSession.collection_ids`).
- Backend: `construction_os/domain/collection.py`, `construction_os/collections/` (standard, validation, loader, markdown_io), `api/collections_service.py`, `api/routers/collections.py`, `loading_collections` graph nodes in `chat.py` / `source_chat.py`, prompt blocks in system templates.
- Wired `collection_ids` through chat queue, artifacts, session utils, and frontend types/hooks/components (`CollectionPicker`, `/collections` library + editor).
- User guide: `docs/3-USER-GUIDE/collections.md`; architecture note: `docs/implementation-notes/collections-architecture.md`.
- Deferred follow-ups documented: project ↔ collection edges, AI auto-selection, per-message subsets, provenance, embeddings.

### Docs hygiene
- Removed earlier working folders (checks, critiques, brainstorms, artifacts, assets) used during planning cycles.
- Retained durable numbered docs, `docs/ai-protocols/a2ui/` (was `docs/a2ui/`), new `docs/audits/` scorecards, `docs/implementation-notes/`, and `docs/summaries/`.

## Files Created or Updated

### Chat queue
- `api/chat_queue_service.py`, `api/routers/chat_queue.py`, `commands/chat_queue_commands.py`
- `construction_os/domain/chat_queue.py`, `chat_queue_models.py`, `construction_os/chat/queue_runner.py`
- `construction_os/database/migrations/36.surrealql`, `37.surrealql` (+ downs)
- `frontend/src/components/source/ChatQueuePanel.tsx`, `frontend/src/lib/hooks/useChatQueue.ts`, `frontend/src/lib/api/chat-queue.ts`
- `tests/test_chat_queue_*.py`, `.cursor/rules/api-server-running.mdc`

### A2UI
- `construction_os/graphs/a2ui_emit.py`, `frontend/src/lib/a2ui/*`, `frontend/src/components/a2ui/*`
- `docs/ai-protocols/a2ui/agent-catalog.md`, `components.md`, `extending.md`

### Insights removal
- `construction_os/database/migrations/39.surrealql` (+ down); removed insights router/service/hooks/dialogs
- Broad updates across chat/source/context graphs, prompts, locales, numbered docs, `README.md`
- `scripts/verify_migration_39.py`

### Frontend architecture (shared primitives)
- `frontend/src/components/common/PickerDialogShell.tsx`, `FormDialogShell.tsx`, `PageError.tsx`, `CheckboxList.tsx`, `CompactListRow.tsx`
- `frontend/src/lib/hooks/useListSelection.ts`, `useChatRuntime.ts`, `useSelectableRow.ts`, `bindChatPanelProps.ts`
- `frontend/src/components/artifacts/ArtifactListRow.tsx`, `ArtifactViewerDialog.tsx`
- `frontend/src/components/settings/McpAuthFields.tsx`, `ChatPanelSkeleton.tsx`
- `docs/audits/2026-07-16-frontend-architecture-score-*.md` (64 → 72 → 82 → 87)

### Dead code cleanup
- Deleted `api/*_service.py` orphans and `api/client.py`
- `.github/workflows/test.yml` (F401/knip jobs), `.cursor/rules/{absolute-imports,dry-principles,strict-typing}.mdc`
- `docs/audits/dead_code_redundancy_audit.md`

### Collections
- `construction_os/database/migrations/40.surrealql`, `41.surrealql` (+ downs)
- `construction_os/domain/collection.py`, `construction_os/collections/*`
- `api/collections_service.py`, `api/collection_models.py`, `api/routers/collections.py`
- `frontend/src/app/(dashboard)/collections/`, `CollectionPicker.tsx`, chat/artifact `collection_ids` wiring
- `docs/3-USER-GUIDE/collections.md`, `docs/implementation-notes/collections-architecture.md`

## Key Decisions

- Chat queue is durable (SurrealDB + shared SQLite checkpoints); API and surreal-commands worker must share the same checkpoint file.
- A2UI is opt-in via dual env flags; protocol pinned to A2UI v0.9 / AskUser + Basic catalog over AG-UI.
- Insights are fully removed (not deprecated); migration 39 is the schema source of truth; product copy is artifacts → notes.
- Frontend remediation favors shared shells (`PickerDialogShell`, `FormDialogShell`) and thin chat adapters (`useChatRuntime`, `bindChatPanelProps`) over feature-local duplication.
- API layer simplified: routers → domain directly; orphan `*_service.py` wrappers deleted.
- Collections mirror the Skills stack (typed manifest + session attachment); graph injects `ACTIVE COLLECTIONS` context block; project/collection graph edges and AI auto-select deferred.
- Durable audit artifacts live in `docs/audits/`; ephemeral working folders stay deleted unless user requests them.

## Issues Found

- A2UI and legacy HTML-template interactive patterns may coexist until A2UI covers more flows.
- Chat queue requires both API and worker healthy; idle pending items usually mean the worker is not running.
- Dead-code audit RED-002: project vs source chat parallel stacks (~2.5k LOC forked) remains intentional-but-incomplete consolidation.
- Post–UI-049–061 remediations landed after the 87 score audit; no follow-up scorecard published yet.
- Collections lack project graph edges, semantic discovery, and per-message item subset UI (documented deferrals).

## Next Steps

- Re-score frontend architecture after UI-049–061 fixes (expect ≥87, possibly 90+).
- Smoke-test Collections end-to-end (create collection → attach in chat → verify `loading_collections` context in session).
- Confirm migrations 40–41 applied on local/dev SurrealDB (add `scripts/verify_migration_40.py` or extend existing verifier).
- Decide next A2UI surfaces beyond AskUser (tool-result cards vs richer catalogs).
- Continue RED-002 chat stack consolidation (shared hooks/API paths for project vs source).
- Commit any remaining dead-code orphan frontend modules flagged in audit (RED-022–024) if still present.

## Notes for Future Agents

- Today’s summary slug: `docs/summaries/2026-07-15.md`. Branch: `dev` (clean working tree at wrap-up).
- Chat queue: probe `http://127.0.0.1:5055/health`; API with `API_RELOAD=false`; worker `python -m surreal_commands.cli.worker --import-modules commands`. Smoke: `python scripts/smoke_chat_queue_a2ui.py`.
- A2UI: enable `A2UI_CHAT_ENABLED` (API) and `NEXT_PUBLIC_A2UI_CHAT` (frontend) together; emit path `construction_os/graphs/a2ui_emit.py`; primary UX is AskUser.
- Insights are gone — do not reintroduce; verify with `python scripts/verify_migration_39.py`. Legacy context mode `"insights"` → full is compatibility only.
- Frontend shared primitives to reuse: `ConfirmDialog`, `FieldError`, `PageRefreshButton`, `PickerDialogShell`, `FormDialogShell`, `PageError`, `CheckboxList`, `useChatRuntime`, `bindChatPanelProps`, `useSelectableRow`, `useListSelection`.
- Collections: domain `construction_os/domain/collection.py`; API `api/routers/collections.py`; session field `collection_ids`; graph node `loading_collections`; user docs `docs/3-USER-GUIDE/collections.md`.
- API services under `api/*_service.py` were intentionally removed — add logic in domain or thin router helpers, not new service wrappers.
- Dead-code baseline: `docs/audits/dead_code_redundancy_audit.md` (81/100). Frontend architecture baseline: `docs/audits/2026-07-16-frontend-architecture-score-87.md`.
