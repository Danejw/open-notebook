# Daily Summary — 2026-07-16

## AI protocol study docs

Protocol study/extend notes live under `docs/ai-protocols/`:

- `a2ui/` — A2UI catalog, components, extending
- `agui/` — AG-UI events, CUSTOM catalog, extending
- `mcp/client/` — Cos-as-MCP-client architecture, authorization, extending

Clarified vs Cos-as-MCP-server docs in `docs/5-CONFIGURATION/mcp-integration.md`.

## Notes → Project Artifacts rename (domain + docs + tests)

Product language now distinguishes **project artifacts** (persisted outputs in a project) from **artifact templates** (global prompt library).

### Domain & API (already implemented)

- Domain model: `ProjectArtifact` (`Note` alias retained); Surreal table remains `note`, IDs `note:…`
- Kinds: `manual` | `ai` | `generated` (migration 42 normalizes legacy `human` / `note` / `artifact`)
- Canonical API: `/api/project-artifacts`; `/api/notes` deprecated aliases
- Response: `artifact_kind` + deprecated `note_type` (same value)
- Templates: `ArtifactTemplate`, `/api/artifact-templates`; `/api/artifacts` deprecated
- Context/search/rebuild accept `artifacts` with legacy `notes` maps

### Documentation updated

- Renamed core concept doc to `docs/2-CORE-CONCEPTS/projects-sources-artifacts.md`
- MCP integration, user guide (artifacts, search), CLAUDE.md files, collections architecture note

### Tests updated

- Prefer `/project-artifacts` routes; keep `/notes` alias smoke coverage
- Kinds use `manual` / `ai` / `generated`; PDF export limited to `generated`
- Chat context tests cover `artifacts` map alongside legacy `notes`
