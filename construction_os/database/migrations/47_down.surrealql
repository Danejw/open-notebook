-- Migration 47 down: remove repaired artifact chat-default fields

REMOVE FIELD IF EXISTS lifecycle_phase ON TABLE artifact;
REMOVE FIELD IF EXISTS skill_ids ON TABLE artifact;
REMOVE FIELD IF EXISTS collection_ids ON TABLE artifact;
REMOVE FIELD IF EXISTS mcp_tool_ids ON TABLE artifact;
REMOVE FIELD IF EXISTS html_template_id ON TABLE artifact;

RETURN true;
